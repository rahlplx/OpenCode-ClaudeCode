Run `vibe-stack auto` to execute the full pipeline state machine: think → plan → break → build → harness → review → ship → retro → learn → evolve → done. Automatically advances through each phase.

$ARGUMENTS
