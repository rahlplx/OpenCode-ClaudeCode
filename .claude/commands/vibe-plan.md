Run `vibe-stack plan` to execute multi-perspective review of the current project state. Cover: risk assessment, acceptance criteria, and dependency mapping. Read from `.vibe/state.json` and update it.

$ARGUMENTS
